# Feature Map — Figma 设计导出（归档）

本目录为从 Figma/Make 导出的 **Vite + React** 参考工程，已纳入在线特征平台仓库，仅作 **UI 与交互实现参考**。

- **非**生产构建入口；正式产品行为与字段以 [`../../design/front-design/产品原型图.md`](../../design/front-design/产品原型图.md) §3.4 及 [`../../design/feature-map-figma-vs-spec.md`](../../design/feature-map-figma-vs-spec.md) 为准。
- 与《产品原型图》或当前 `FEATURE_STORE.html` 不一致之处见差异清单文档。

核心实现路径：`src/app/App.tsx`，业务组件在 `src/app/components/feature-map/`。

---

## 原始说明（导出自带）

This is a code bundle for Feature Map. The original project is available at https://www.figma.com/design/Bwyhi6WYSjtohISRQ8X7kY/Feature-Map.

### Running the code

Run `npm i` to install the dependencies.

Run `npm run dev` to start the development server.
