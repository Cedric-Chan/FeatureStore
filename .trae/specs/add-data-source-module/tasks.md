# Tasks
- [x] Task 1: 复制 Figma 导出的页面组件：将外部的 `DataSourceMappingPage.tsx` 复制到项目中的 `src/app/pages/` 目录下。
- [x] Task 2: 更新 App 路由：在 `src/app/App.tsx` 中引入 `DataSourceMappingPage`，并在 `<Route element={<AppShell />}>` 下添加 `/ds` 路由。
- [x] Task 3: 更新侧边栏导航：在 `src/app/layout/AppShell.tsx` 中的 `NAV_ITEMS` 数组里，将 "Data Source" (指向 `/ds`) 插入到 "Feature Source" 之前。
